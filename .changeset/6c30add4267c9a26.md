---
---

Remove the "Manage AAO" tier and consolidate all manage pages under /admin with requireAdmin auth. Kitchen cabinet members no longer have separate access. Old /manage/* URLs redirect to /admin/* preserving query strings.
