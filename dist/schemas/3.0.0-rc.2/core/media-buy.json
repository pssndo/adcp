{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "/schemas/3.0.0-rc.2/core/media-buy.json",
  "title": "Media Buy",
  "description": "Represents a purchased advertising campaign",
  "type": "object",
  "properties": {
    "media_buy_id": {
      "type": "string",
      "description": "Publisher's unique identifier for the media buy"
    },
    "buyer_ref": {
      "type": "string",
      "description": "Buyer's reference identifier for this media buy. Sellers SHOULD deduplicate requests with the same buyer_ref and account, returning the existing media buy rather than creating a duplicate."
    },
    "buyer_campaign_ref": {
      "type": "string",
      "description": "Buyer's campaign reference label. Groups related operations under a single campaign for CRM and ad server correlation."
    },
    "account": {
      "$ref": "/schemas/3.0.0-rc.2/core/account.json",
      "description": "Account billed for this media buy"
    },
    "status": {
      "$ref": "/schemas/3.0.0-rc.2/enums/media-buy-status.json"
    },
    "rejection_reason": {
      "type": "string",
      "description": "Reason provided by the seller when status is 'rejected'. Present only when status is 'rejected'."
    },
    "total_budget": {
      "type": "number",
      "description": "Total budget amount",
      "minimum": 0
    },
    "packages": {
      "type": "array",
      "description": "Array of packages within this media buy",
      "items": {
        "$ref": "/schemas/3.0.0-rc.2/core/package.json"
      }
    },
    "creative_deadline": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp for creative upload deadline"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "Creation timestamp"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "Last update timestamp"
    },
    "ext": {
      "$ref": "/schemas/3.0.0-rc.2/core/ext.json"
    }
  },
  "required": [
    "media_buy_id",
    "status",
    "total_budget",
    "packages"
  ],
  "additionalProperties": true
}
